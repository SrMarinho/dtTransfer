$schema: ../../../schemas/workspace.json
id: biSenior
kind: yaml

sources:
  - name: Senior
    driver: oracle
    env_prefix: DB_SENIOR_ORACLE

target:
  name: biSenior
  driver: postgres
  env_prefix: DB_BISENIOR_POSTGRES
