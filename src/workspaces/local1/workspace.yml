$schema: ../../../schemas/workspace.json
id: local1
kind: yaml

sources:
  - name: biMktNaz
    driver: postgres
    env_prefix: DB_BIMKTNAZ_POSTGRES

target:
  name: local
  driver: postgres
  env_prefix: LOCAL
